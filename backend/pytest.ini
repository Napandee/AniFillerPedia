[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    live_network: hits the real, public AniList GraphQL API. Auto-skipped when
        that API is unreachable, so a third-party outage cannot block CI.
