# Pydantic request/response models — the shapes FastAPI's auto-generated
# OpenAPI schema exposes, which the frontend's typed client (issue #11) is
# generated from. Empty scaffolding for now — populated as endpoint issues
# (#7, #8, #12, #13, #14) land; out of scope for #6 itself.
