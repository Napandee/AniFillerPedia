node_modules/
# Intermediate fetch artifact — regenerated by `npm run generate:api-client`
# every run, not meant to be committed (schema.d.ts, the actual generated
# output, IS committed — see README.md).
src/api/openapi.json

dist/
.astro/
.env
