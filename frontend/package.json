{
  "name": "anifillerpedia-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "AniFillerPedia Astro frontend — Playful Fandom design system, typed API client wiring (#11), and the page/routing shell every other Phase 5 issue builds on.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "generate:api-client": "node scripts/fetch-schema.mjs && openapi-typescript src/api/openapi.json -o src/api/schema.d.ts"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "openapi-typescript": "^7.5.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@astrojs/node": "^11.1.4",
    "astro": "^7.2.8",
    "marked": "^14.1.4",
    "openapi-fetch": "^0.13.4"
  }
}
