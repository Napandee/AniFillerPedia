---
// #106: thin per-locale route wrapper — the real page lives in
// components/pages/HomePage.astro so it's written once and shared across
// every locale's own route file (Astro's i18n routing requires an actual
// page file per locale, it doesn't auto-duplicate routes from config
// alone). See src/pages/{es,hi,ja,zh-cn}/index.astro for the identical
// pattern.
import HomePage from "../components/pages/HomePage.astro";
---

<HomePage />
